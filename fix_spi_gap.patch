--- ok/Core/Src/main.c.orig	2024-11-13 10:00:00.000000000 +0000
+++ ok/Core/Src/main.c	2024-11-13 15:30:00.000000000 +0000
@@ -25,6 +25,7 @@

 volatile uint8_t adc_complete = 0;
 volatile uint8_t spi_busy = 0;
+volatile uint8_t spi_complete = 0;
 volatile uint8_t nss_triggered = 0;
 volatile uint32_t adc_count = 0;
 volatile uint32_t spi_count = 0;
@@ -71,10 +72,9 @@

 void HAL_SPI_TxRxCpltCallback(SPI_HandleTypeDef *hspi)
 {
-  spi_busy = 0;
+  spi_complete = 1;
   spi_count++;
-  // Restart SPI ngay
-  HAL_SPI_TransmitReceive_DMA(&hspi1, tx_buffer, rx_dummy, TX_BYTES);
+  // Do NOT restart here - will restart in main loop
 }

 void HAL_SPI_ErrorCallback(SPI_HandleTypeDef *hspi)
@@ -130,6 +130,13 @@
       HAL_GPIO_TogglePin(GPIOC, GPIO_PIN_13);
     }

+    // Restart SPI when transfer complete
+    if (spi_complete) {
+      spi_complete = 0;
+      HAL_SPI_TransmitReceive_DMA(&hspi1, tx_buffer, rx_dummy, TX_BYTES);
+    }
+
+    // Restart ADC when conversion complete
     if (adc_complete) {
       adc_complete = 0;
       start_adc_capture();
